[package]
name = "rust_shell"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
nix = { version = "0.28", features = ["process", "fs"] }
once_cell = "1.19"
